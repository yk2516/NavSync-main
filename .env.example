# NavSync 环境变量配置参考
#
# ⚠️ 这些变量请在 Cloudflare Pages Dashboard 中设置，不要写在 .env 文件里！
#    Pages Dashboard → 你的项目 → Settings → Environment variables
#
# 共 5 项配置，分两类：
#
# ── 环境变量 - 云端同步（Settings → Environment variables）──
#   GITHUB_TOKEN   - GitHub Personal Access Token（必填，仅需 gist 权限）
#                    创建: https://github.com/settings/tokens/new?description=NavSync%20Cloud%20Sync&scopes=gist
#   CLOUD_PASSWORD - 访问口令（**强烈建议必填**）
#                    它同时承担两个职责：
#                      1) 云端同步口令：上传/下载/查找配置时的鉴权
#                      2) 管理口令：只有输入它的人才能看到设置页
#                        （云端同步、重置数据、导入导出）
#                    留空 = 关闭口令保护 = 任何人都能同步、也能进设置页，不要留空
#
# ── 环境变量 - Favicon（Settings → Environment variables）──
#   FAVICON_SOURCE - 可选，favicon 第三方图标源：google（默认）/ duckduckgo / 0x3
#   FAVICON_TTL    - 可选，缓存时长（秒），范围 60 ~ 2592000，默认 2592000（30 天，KV 上限）
#
# ── KV 绑定（Settings → Bindings，非环境变量）──
#   FAVICON_KV     - 必填，favicon 代理缓存命名空间
#                    KV 命名空间名称：建议 navsync-favicon（可取其他名字）
#                    绑定变量名：必须填 FAVICON_KV（代码中按此名称读取，不可改）
#
# ── 操作步骤 ──
# 1. 创建 KV 命名空间：Workers & Pages → KV → Create namespace
#    名称建议填 navsync-favicon，点 Add
# 2. 绑定：Pages Dashboard → 你的项目 → Settings → Bindings → Add binding
#    - Type：KV namespace
#    - Variable name（binding）：FAVICON_KV
#    - KV namespace：选择刚创建的 navsync-favicon
# 3. 若部署完成后再补绑定，需到 Deployments 页面点 Redeploy 重新部署才会生效
#
# ── 访问权限模型（v2）──
# 站长（知道 CLOUD_PASSWORD 的人）
#   · 首页看到自己的导航
#   · 可进入 /setting：云端同步 / 重置 / 导入导出
#   · 可增删改站点、拖拽排序
# 访客（其他所有人）
#   · 看不到设置齿轮，/setting 只有一个口令输入框
#   · 首页直接渲染站长已同步到云端的导航配置（不是内置的初始预设页）
#   · 全程只读：不能编辑、不能拖拽、不能同步、不能重置
#
# 数据流向：
#   站长本地 → 上传到云端(GitHub Gist) → /api/public-config 只读转发 → 访客首页
# 所以站长修改完导航后，记得在 /setting 点一次「上传到云端」，访客端才会更新。
